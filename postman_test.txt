การทดสอบ API `/analyze-image` และ `/analyze-video` ด้วย **Postman** ทำได้ตามขั้นตอนด้านล่าง โดยคุณต้องมี **API Key** ก่อน (เช่น จาก `/request-api-key` หรือจากการซื้อแพ็กเกจ)

---

## 🔑 1. เตรียม API Key
- หากยังไม่มี API Key:
  - สมัครและล็อกอินผ่าน `/signup` และ `/login`
  - ขอ API Key แบบฟรี: POST `/request-api-key` พร้อม body เช่น:
    ```json
    {
      "analysis_types": ["cigarette", "porn"],
      "plan": "test"
    }
    ```
  - ระบบจะตอบกลับ `apiKey` ที่ใช้ได้ 7 วัน

---

## 📸 2. ทดสอบ `/analyze-image`

### ✅ Request
- **Method**: `POST`
- **URL**: `http://localhost:5000/analyze-image` (หรือ URL จริงของคุณ)
- **Headers**:
  - `x-api-key`: `<your_api_key>`
- **Body**: `form-data`
  - `images`: เลือกไฟล์ภาพ (เช่น `.jpg`, `.png`) หรือ `.zip` ที่มีหลายภาพ
  - `analysis_types` *(optional)*: เช่น `["cigarette","weapon"]` หรือ `cigarette,weapon`
  - `thresholds` *(optional)*: เช่น `{"cigarette":0.4,"weapon":0.6}`
  - `output_modes` *(optional)*: เช่น `["bbox","blur"]` หรือ `bbox,blur`

> ⚠️ ถ้าไม่ส่ง `analysis_types` จะใช้ค่าจาก API Key ที่บันทึกไว้

### 📥 ตัวอย่าง Postman Setup
| Key | Value |
|-----|-------|
| images | (เลือกไฟล์ภาพ) |
| analysis_types | `["cigarette"]` |
| thresholds | `{"cigarette":0.3}` |
| output_modes | `["bbox","blur"]` |

---

## 🎥 3. ทดสอบ `/analyze-video`

### ✅ Request
- **Method**: `POST`
- **URL**: `http://localhost:5000/analyze-video`
- **Headers**:
  - `x-api-key`: `<your_api_key>`
- **Body**: `form-data`
  - `video`: เลือกไฟล์วิดีโอ (เช่น `.mp4`)
  - `analysis_types` *(optional)*: เช่น `["cigarette"]`
  - `thresholds` *(optional)*: เช่น `{"cigarette":0.5}`

> ⚠️ ไฟล์วิดีโอต้องเป็นนามสกุลที่รองรับ: `.mp4`, `.mov`, `.avi`, `.mkv`, `.webm`

### 📥 ตัวอย่าง Postman Setup
| Key | Value |
|-----|-------|
| video | (เลือกไฟล์ .mp4) |
| analysis_types | `["cigarette"]` |
| thresholds | `{"cigarette":0.4}` |

---

## 📌 หมายเหตุสำคัญ
- ตรวจสอบว่าโมเดลที่ระบุใน `analysis_types` มีอยู่จริงในระบบ (เช่น `"cigarette"`, `"porn"`, `"weapon"`, `"violence"`)
- หาก API Key ไม่รองรับ media type (เช่น ใช้ key ที่สร้างสำหรับ image เท่านั้น แล้วส่งวิดีโอ) → จะได้ error `403`
- ไฟล์ที่อัปโหลดจะถูกลบอัตโนมัติหลัง 1 ชั่วโมง
- วิดีโอจะประมวลผลทุก `N` เฟรม (ตาม `VIDEO_FRAME_SKIP` ใน env)

---

หากคุณต้องการให้ช่วยสร้าง **Postman Collection JSON** สำหรับ import ตรงๆ ก็บอกได้นะครับ!